<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.ssafy.project.dao.CampSiteDao">

    <!-- 캠핑장 전체 리스트 -->
	<select id="campSiteList" resultType="com.ssafy.project.dto.CampSiteDto">
		SELECT *
		FROM campSite;
	</select>

    <!-- 캠핑장 검색 리스트 -->
	<select id="campSiteSearch" parameterType="String" resultType="com.ssafy.project.dto.CampSiteDto">
		SELECT *
		FROM campSite
        WHERE facltNm LIKE CONCAT('%', #{searchWord}, '%');
	</select>

	<!-- 도 이름을 가져온다 -->
	<select id="campSiteDo" resultType="String">
		SELECT DISTINCT doNm
		FROM campSite;
	</select>

	<!-- 도에 해당하는 시, 군, 구 이름을 가져온다 -->
	<select id="campSiteSigungu" parameterType="String" resultType="String">
		SELECT DISTINCT sigunguNm
		FROM campSite
        WHERE doNm = #{doNm};
	</select>

</mapper>